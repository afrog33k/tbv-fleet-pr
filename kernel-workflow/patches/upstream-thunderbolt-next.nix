# Empty: the 0101-0121 maintainer-tree series is upstream in v7.2-rc7.
#
# The regen script `regen-upstream-thunderbolt-patches.sh` is kept around
# to track future maintainer delta (v7.3 merge window), but the patch set
# itself is no longer needed against any currently supported stock kernel.
[ ]
