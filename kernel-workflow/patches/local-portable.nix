# Project-local patches that apply on top of the upstream maintainer tree
# (or, on a stock kernel at >= v7.2-rc7, on top of the kernel itself with
# the maintainer delta already merged in).
import ./7.2-rc7.nix
